<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:layout_marginHorizontal="@dimen/screen_horizontal_padding_small"
    android:layout_marginTop="@dimen/screen_top_padding"
    android:layout_marginBottom="@dimen/screen_bottom_padding"
    tools:ignore="ContentDescription">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/logo"
            style="@style/Text.Default.White"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:layout_marginHorizontal="@dimen/logo_padding_big"
            android:src="@drawable/ic_logo" />

        <View
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="2" />

        <com.arrive.terminal.core.ui.view.AppInputView
            android:id="@+id/input"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/screen_horizontal_padding_medium" />

        <View
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <com.arrive.terminal.core.ui.view.AppButtonView
            android:id="@+id/login"
            style="@style/Button.Primary"
            android:layout_marginHorizontal="@dimen/screen_horizontal_padding_medium"
            android:layout_width="match_parent"
            android:layout_height="@dimen/default_button_height"
            tools:text="@string/login_to_driver_action" />

    </LinearLayout>

</ScrollView>